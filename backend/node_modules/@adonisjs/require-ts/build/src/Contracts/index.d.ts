/**
 * Custom transformers extracted from the package.json file
 */
export declare type Transformers = {
    before?: {
        transform: string;
    }[];
    after?: {
        transform: string;
    }[];
    afterDeclarations?: {
        transform: string;
    }[];
};
